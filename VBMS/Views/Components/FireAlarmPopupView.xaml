<UserControl x:Class="VBMS.Views.Components.FireAlarmPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="340" Height="Auto"
             Background="Transparent">

    <!-- 카드 전체 외곽 그림자 & 테두리 -->
    <Border CornerRadius="12"
            Background="White"
            BorderBrush="#D32F2F"
            BorderThickness="1.5"
            Margin="4">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.2" ShadowDepth="3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- 1. 타이틀 헤더 -->
                <RowDefinition Height="*"/>
                <!-- 2. 경고 메시지 본문 -->
                <RowDefinition Height="Auto"/>
                <!-- 3. 하단 액션 버튼 -->
            </Grid.RowDefinitions>

            <!-- 1. 긴급 상단 경고 헤더 -->
            <Border Grid.Row="0" 
                    Background="#D32F2F" 
                    CornerRadius="10,10,0,0"
                    Padding="14,10">
                <Grid>
                    <!-- 좌측: 불꽃 아이콘 + 타이틀 -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                        <materialDesign:PackIcon Kind="Fire" Width="20" Height="20" Foreground="White" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="{Binding Title, FallbackValue='연기 감지'}" 
                                   Foreground="White" 
                                   FontWeight="Bold" 
                                   FontSize="15" 
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- 우측: [X] 닫기 버튼 -->
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                            Command="{Binding CloseCommand}"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Width="26" Height="26" Padding="0"
                            Foreground="White" Opacity="0.9">
                        <materialDesign:PackIcon Kind="Close" Width="18" Height="18"/>
                    </Button>
                </Grid>
            </Border>

            <!-- 2. 경고 메시지 본문 영역 -->
            <Border Grid.Row="1" Padding="14">
                <Border Background="#FFEBEE" 
                        BorderBrush="#FFCDD2" 
                        BorderThickness="1" 
                        CornerRadius="8" 
                        Padding="12,10">
                    <DockPanel>
                        <materialDesign:PackIcon Kind="AlertCircle" 
                                                 Foreground="#D32F2F" 
                                                 Width="20" Height="20" 
                                                 Margin="0,2,8,0" 
                                                 DockPanel.Dock="Left" 
                                                 VerticalAlignment="Top"/>
                        <TextBlock Text="{Binding Message}" 
                                   Foreground="#B71C1C" 
                                   FontWeight="Bold" 
                                   FontSize="13" 
                                   LineHeight="20"
                                   TextWrapping="Wrap"/>
                    </DockPanel>
                </Border>
            </Border>

            <!-- 3. 하단 버튼 영역 -->
            <Border Grid.Row="2" 
                    Background="#F5F5F5" 
                    CornerRadius="0,0,10,10"
                    Padding="14,10">
                <Button Command="{Binding CloseCommand}"
                        Style="{StaticResource MaterialDesignFlatMidBgButton}"
                        Background="#D32F2F"
                        Foreground="White"
                        FontWeight="Bold"
                        Height="32"
                        Width="84"
                        HorizontalAlignment="Right"
                        materialDesign:ButtonAssist.CornerRadius="6">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Check" Width="16" Height="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="확인" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Border>
        </Grid>
    </Border>
</UserControl>